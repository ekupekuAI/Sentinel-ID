// Next 16.3 generates references to this internal JavaScript module without
// shipping its declaration file. Keep the workaround local until Next provides it.
declare module 'next/dist/build/segment-config/app/app-segment-config.js' {
  export type InstantConfigForTypeCheckInternal = boolean | Record<string, unknown>;
  export type Prefetch = 'auto' | 'partial' | 'unstable_eager' | 'force-disabled';
}
